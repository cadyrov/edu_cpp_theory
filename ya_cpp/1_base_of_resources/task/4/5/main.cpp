/*5. **`Document`: скрытые зависимости и смена реализации.** С нуля реализуйте копируемый и
 * перемещаемый документ с операциями добавления строки, получения строки по индексу, подсчёта строк
 * и формирования полного текста. Сначала храните строки в `std::vector<std::string>`, затем
 * замените его на `std::deque<std::string>`, не меняя публичный заголовок и клиентский код.
 * Контейнер и вспомогательные алгоритмы должны находиться только в `.cpp`. Соберите реализацию и
 * два клиентских `.cpp` как отдельные единицы трансляции; по выводу инкрементальной сборки сравните
 * пересборку после изменения `Impl` и публичного интерфейса. Объясните, какие зависимости PImpl
 * скрывает, какие остаются из-за сигнатур методов и почему одной этой идиомы недостаточно для
 * обещания совместимости любых версий библиотеки.*/
#include "document.h"

#include <iostream>

int main() {
    Document d{};

    d.AddLine("kjnkljn");
    d.AddLine("csdlkjlkmcds");
    d.AddLine("csdvdvsvd");

    Document d_1(d);
    d_1.AddLine("pokmoimim");

    Document d_2(std::move(d));

    std::cout << d << "\n";
    std::cout << d_1[1] << " " << d_1.size() << "\n";

    std::cout << d_1 << "\n";
    std::cout << d_2 << "\n";

    return 0;
}