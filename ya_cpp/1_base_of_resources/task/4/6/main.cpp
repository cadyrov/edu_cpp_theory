/*6. **`FileWriter`: ресурс внутри реализации.** С нуля создайте некопируемый, перемещаемый класс с
 * операциями открытия файла, записи строки, закрытия и `is_open() const`. Спрячьте файловый поток и
 * обработку ошибок в `Impl`; ресурс должен закрываться автоматически при разрушении реализации.
 * Задайте поведение при повторном закрытии, записи в закрытый объект, ошибке открытия и
 * использовании объекта после перемещения. Продемонстрируйте успешную запись, ошибку открытия,
 * перемещение и move assignment поверх уже открытого файла. Объясните, какие обязанности выполняют
 * PImpl, `unique_ptr` и RAII самого файлового потока.*/

int main() {}