Вы разрабатываете библиотеку для сжатия изображений, которая использует новейшие разработки вашей компании. 

Попытайтесь скрыть реализацию алгоритма, чтобы конкуренты не смогли её украсть.

Скопируйте код ниже к себе и примените паттерн PImpl (Pointer to Implementation):

- Реализуйте класс ImageEncoder, который использует внутренний класс Impl.
- Примените std::experimental::propagate_const.
- Реализуйте три метода класса Impl:
```
Неконстантный метод Encode(), который вызывает std::this_thread::sleep_for(std::chrono::seconds(1));.
Константный метод GetFormat(), который всегда возвращает строку "PNG".
Константный метод GetQuality(), который всегда возвращает значение 42.
```

При объявлении константных методов используйте ключевое слово noexcept.

```cpp
// ====== ImageEncoder.hpp ======

class ImageEncoder {
    // Ваш код для .hpp файла здесь
};

// ====== ImageEncoder.cpp ======

// Ваш код для cpp-файла здесь 
```

- Используйте std::make_unique для инициализации объекта impl_.
- Не забудьте, что GetFormat и GetQuality должны быть const.
- Обязательно добавьте noexcept в неконстантные методы.
- В классе Impl можно возвращать простые заглушки (например "PNG" и 42).