Скопируйте код ниже к себе и допишите его так, чтобы он компилировался и работал корректно. Семантику функции process не меняйте.
```cpp
void process(std::span<int> s) {
  std::for_each(s.begin() s.end(),
    [](auto& x) {
      x *= x;
  });
};

void main() {
  std::vector v = {1, 2, 3};
  process(/*ваш код здесь*/);
  assert((v == std::vector{1, 4, 9}));
} 
```
- Вспомните, что можно получить std::span из данных вектора.

## Задание 2
Реализуйте функцию печати вектора значений как матрицы 3x3 с помощью представления std::mdspan.

```cpp
void print(/**/) {
  // ваш код здесь
};

int main() {
  std::vector v = {1, 2, 3, 4, 5, 6, 7, 8, 9};
  print(/*ваш код здесь*/);

  return 0;
} 
```

- В mdspan можно указать измерения и их размерности.
- Для получения размерности можно использовать метод extent в цикле.