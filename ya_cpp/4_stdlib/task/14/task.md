Представьте, что ваша компания разрабатывает софт для настройки оборудования сетевых фабрик. У каждого устройства множество параметров конфигурации — от идентификаторов компонентов до сложных текстовых шаблонов для журнала состояния.

Ваш предшественник реализовал систему хранения таких параметров, но без учёта производительности: поиск занимал много времени, создавалось множество временных объектов. Вас попросили улучшить работу с коллекциями конфигураций — сделать их быстрыми и гибкими, сохранив строгую проверку по структуре идентификаторов.

Скопируйте код ниже к себе и реализуйте систему хранения параметров конфигурации с эффективным поиском:
- В структуре Id добавьте оператор <=> по умолчанию.
- В HeavyObject:
- - Определите оператор <=>, упорядочивающий объекты сначала по name, затем по id.

- Реализуйте гетерогенные операторы <=> только для std::string_view, которые сравнивают с полем name.
- Создайте std::set<HeavyObject, std::less<>> и вставьте три параметра:
```cpp
{1,2,3}, имя "GPU_Param"
{4,5,6}, имя "GPU_Param"
{7,8,9}, имя "CPU_Param"
```
Выполните три операции разными методами:
- Поиск параметра с id = {7,8,9} через find + вывод сообщения
- Проверка существования параметра с именем "CPU_Param" через contains
- Подсчёт количества параметров с именем "GPU_Param" через count

```cpp
#include <array>
#include <print>
#include <set>
#include <string>
#include <string_view>

struct Id {
    int part1, part2, part3;
    // Добавьте оператор сравнения здесь
};

struct HeavyObject {
    std::array<int, 128> data{};
    Id id;
    std::string name;
    // Реализуйте операторы сравнения здесь
};

int main() {
    // Создайте множество и вставьте элементы здесь

    if (/* Поиск по id через find */) {
        std::println("Found element with id ID(7, 8, 9)");
    }

    std::println("Has element with name=CPU_Param: {}", /* Проверка через contains */);

    std::println("Num of elements with name=GPU_Param: {}", /* Подсчёт через count */);
} 
```

- Используйте std::tie(name, id) для создания составного ключа упорядочения.
- Гетерогенные операторы <=> должны сравнивать string_view только с полем name.
- Для поиска по id реализуйте гетерогенное сравнение, «дополняя» недостающую часть ключа (name) значением из левого/правого операнда.
- Обязательно используйте std::less<> (без параметров) как компаратор множества, чтобы включить поддержку гетерогенного поиска.

## Задание 2
Вам нужно модернизировать хранилище конфигураций на основе `std::map`.
Ключ `MyKey` содержит числовой идентификатор и текстовое описание, но упорядочение
и уникальность ключей определяются только полем `id`.

Скопируйте код ниже и выполните задания:
- Определите прозрачный компаратор `TransparentCmp`.
- Компаратор должен сравнивать `MyKey` с `MyKey`, `MyKey` с `int` и `int` с `MyKey` по полю `id`.
- Добавьте в компаратор псевдоним `is_transparent`, чтобы `std::map` поддерживал поиск непосредственно по `int` без создания временного `MyKey`.
- Создайте `std::map` с ключом `MyKey`, значением `std::pair<int, std::string>` и компаратором `TransparentCmp`.
- Заполните карту 100 000 элементов четырьмя способами, по 25 000 элементов каждым:
  - `insert`;
  - `emplace(k, v)`;
  - `emplace(std::piecewise_construct, ...)`;
  - `try_emplace(k, v)`.
- Для каждого элемента используйте идентификатор `i` и строку `"dev-" + std::to_string(i)`.
- Найдите элемент по числу `56'789` с помощью `find`.
- Проверьте наличие идентификатора `12'345` с помощью `contains`.


```cpp
#include <map>
#include <print>
#include <string>
#include <tuple>
#include <utility>

struct MyKey {
    int id;
    std::string textId;
};

struct TransparentCmp {
    // Добавьте is_transparent и три перегрузки operator()
};

int main() {
    // Создайте std::map<MyKey, std::pair<int, std::string>, TransparentCmp>

    for (int i = 0; i < 25'000; ++i) {
        // Вставьте элемент через insert
    }

    for (int i = 25'000; i < 50'000; ++i) {
        // Вставьте элемент через emplace(k, v)
    }

    for (int i = 50'000; i < 75'000; ++i) {
        // Используйте emplace(std::piecewise_construct, ...)
    }

    for (int i = 75'000; i < 100'000; ++i) {
        // Вставьте элемент через try_emplace(k, v)
    }

    auto it = /* Найдите элемент по int без создания MyKey */;
    if (it != configMap.end()) {
        std::println("Found by int: {} -> ({}, {})",
                     it->first.id, it->second.first, it->second.second);
    }

    std::println("Contains id 12345: {}", /* Проверка через contains */);
}
```

- Используйте `std::piecewise_construct` только вместе с `std::forward_as_tuple(...)`.
- В операциях `find` и `contains` передавайте `int`.
- Не создавайте временный объект `MyKey` при поиске.
- Не забудьте добавить псевдоним типа `is_transparent`.
