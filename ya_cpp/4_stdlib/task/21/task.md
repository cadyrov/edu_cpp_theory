Задание 1
Представьте, что вы разрабатываете ПО для фитнес-браслетов и вам нужно написать функции для анализа пешей активности пользователя. На основе массива с ежедневными шагами функции должны рассчитывать динамику активности по дням, общее и среднее число шагов и — самое важное — шкалу мотивации, или прогресс текущего дня по сравнению с предыдущими.
Скопируйте код ниже к себе и выполните задания:
Вычислите суммарное и среднее количество шагов.
Найдите день, в который было сделано наибольшее количество шагов.
Преобразуйте line так, чтобы за каждый день в столбец выводилось:
3 символа *, если сделано на 3000 больше шагов чем в среднем;
2 символа *, если сделано шагов ±1000 от среднего значения;
1 символ *, если сделано на 3000 меньше шагов чем в среднем.
Посчитайте разность между каждым днём и средним количеством шагов.
Выведите мотивационную шкалу: если каждый следующий день больше предыдущего — считайте это за +1, иначе 0. Затем посчитайте inclusive_scan, чтобы пользователь видел мотивационный рост.
Используя transform_inclusive_scan, постройте массив мотивационного вклада — преобразуйте каждый день в +1 или -1 (в зависимости от превышения среднего) и проведите накопление.
Выведите финальный отчёт: сумму шагов, среднее значение, медианное значение и индекс самого продуктивного дня.
Прекод:

void AnalyzeSteps() {
    std::vector<int> steps(30);

    std::mt19937 rng(42);
    std::uniform_int_distribution<int> dist(5000, 15000);
    std::generate(steps.begin(), steps.end(), std::bind(dist, rng));

    int totalSt = 0;
    int avrSt = 0;
    int maxSt = 0;
    int dayWithMaxSt = 0;

    // Ваш код здесь
    
    std::print("\nВаш прогресс:\n");
    for (auto target : {avrSt + 3000, avrSt + 500, avrSt - 3000}) {
      std::string line(steps.size(), ' ');
      
      // Ваш код здесь

      std::print("{}\n", line);
    }
    std::print("Дни: от 1 до 30\n\n");

    int meanSt = 0;

    std::print("Ваша статистика за последние 30 дней:"
                "\n\tВсего шагов: {}"
                "\n\tШагов в среднем за день: {}"
                "\n\tШагов в середине месяца: {}"
                "\n\tСамый продуктивный день - {}. Вы совершили {} шагов.\n\n"
                , totalSt, avrSt, meanSt, maxSt, dayWithMaxSt);
    
    
    std::print("Ваш прогресс:\n");
    
    for (auto target : {avrSt + 3000, avrSt + 500, avrSt - 3000}) {
      std::string line(steps.size(), ' ');
      
      // Ваш код здесь

      std::print("{}\n", line);
    }
    std::print("Дни: от 1 до 30\n");
} 
Авторское решение:

Подсказка:

Для вычисления метрик примените алгоритм, который принимает на вход последовательность и возвращает в качестве результата единственное значение.
Используйте алгоритм, который ищет значение элемента на заданной позиции в отсортированном массиве за O(N).
Используйте алгоритм для преобразования всех элементов последовательности при выполнении условия.
Задание 2
Город будущего строит автономную систему управления ливневыми потоками. Вам нужно разработать модуль для оценки эффективности распределения воды на основе рельефа улиц.
Каждая улица представлена массивом высот, и вам нужно оценить:
Сколько воды может скопиться после дождя.
Где находятся зоны максимального скопления воды.
Насколько равномерно распределяется вода между секциями улицы.
Нарисовать график накопления воды по отсекам.
Обратите внимание на пример модели улицы: высота воды всегда равна минимуму между наибольшими границами слева и справа. Например, высота воды над 8-й ячейкой будет определяться как std::min(left_max[8], right_max[8]).
При этом какую бы ячейку мы ни взяли, максимальная высота всегда будет равна максимуму между уже найденной максимальной высотой для предыдущей ячейки и текущей высотой: для левой стороны std::max(left_max[i - 1], height[i]).
Скопируйте код ниже к себбе и выполните задания:
Сгенерируйте рельеф улицы с помощью std::generate со случайными значениями от 0 до 10.
Подумайте, какой алгоритм подойдёт, чтобы заполнить значениями left_max и right_max.
Посчитайте максимумы высот слева и справа и запишите и в left_max, и в right_max.
Определите объём воды, который можно собрать над каждой ячейкой.
Найдите индекс с наибольшим скоплением воды.
Найдите среднее количество воды на улице.
Вычислите равномерность распределения воды по формуле: для каждого элемента посчитайте (cur - avr) ^ 2, полученные значения сложите и разделите на N (выборочная дисперсия).
Прекод:

/*

========= Модель улицы и накопления воды =========
       8
       |
    #~~~~~~~~#~~~~~~~~~#~~~~~~~~~~~~~~~~~~~~#     
   ##~~~~~~~~#~~~~~~#~~#~#~~~~~~~~~~~~~~~~~~#~~#  
  ###~~~~~~~~#~~#~~~#~~#~#~~~#~~~~~##~~~~#~##~~#  
  ###~~~~~~~~#~~#~~##~~#~#~~~#~#~~~##~~~~#~##~~#~#
  ####~~~~####~~#~~##~~#~#~~~#~#~~###~~~##~##~~#~#
  #####~~~####~~#~~##~~#~#~~~#~#~~###~~~##~##~##~#
#~#####~#~####~~#~###~~#~#~~~#~#~~####~~##~#######
#######~######~##~####~###~~~#~##~####~~##~#######
#######~######~#######~###~~~####~####~~##~#######
#################################~####~###~#######
##################################################
==================================================
0                       25                       50

*/

void Simulate() {
    constexpr int N = 50;
    std::vector<int> height(N);
    std::vector<int> left_max(N), right_max(N), water(N);

    std::mt19937 rng(42);
    std::uniform_int_distribution<int> dist(0, 10);

    // Модель
    PrintWaterStreetModel(N, total_height, height, water);

    // Статистика
    std::print("\n\n=== Статистика анализа ===\n");
    std::print("Общий объём воды: {}\n", 0);
    std::print("Среднее на ячейку: {:.2f}\n", 0);
    std::print("Максимум воды в ячейке {}: {} единиц\n", 0, 0);
    std::print("Дисперсия распределения воды: {:.2f}\n", 0);
} 
Авторское решение:

Подсказки:

Используйте inclusive_scan, чтобы получить максимумы слева и справа.
Итоговая формула расчёта воды после получения частичных сумм слева и справа: std::min(left_max[i], right_max[i]) - height[i].