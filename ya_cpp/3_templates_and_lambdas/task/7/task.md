- Проверить предикат на необъединённом в контейнер наборе данных — непростая задача. Стандартные алгоритмы std::any_of, std::all_of и std::none_of работают только с итераторами, а значит, с последовательностями элементов.
- Cоздавать временный контейнер, чтобы просто проверить предикат на каждом конкретном значении, невыгодно, так как это влечёт дополнительные вычислительные траты.
- Поэтому напишите свою функцию, которая по короткой схеме будет определять, для всех ли аргументов функции предикат возвращает true.
- Скопируйте код ниже и реализуйте функцию all_of, которая: 
- - для каждого аргумента из пакета параметров args проверяет, возвращает ли предикат pred для этого вргумента true;
- - работает по короткой схеме: прекращает вычислять значения pred, если попалось хотя бы одно значение false;
проверяет, можно ли вызвать предикат на всех аргументах функции.


```cpp
template <typename Pred, typename... Args>
auto all_of(Pred &&pred, Args &&...args) {
    ? ?
} 
```

- Используйте static_assert, std::is_invocable_r_v и fold expression, чтобы проверить возможность вызвать предикат на аргументах функции во время компиляции.
- Используйте унарную свёртку на короткосхемном операторе &&.
- Используйте идеальную передачу аргументов функции в предикат.