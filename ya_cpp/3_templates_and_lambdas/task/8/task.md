Попробуйте реализовать упрощённую версию std::bind_front, которая будет работать только с указателями на функции и классами с оператором вызова функции. 
```cpp
int fusedMultiplyAdd(int x, int y, int z) { return x * y + z; };

void test_fma() {
    auto mul_42 = simple_bind_front(fusedMultiplyAdd, 42);
    auto result = mul_42(10, 5);
  
  assert(result == 425);
} 
```

Скопируйте код ниже и реализуйте simple_bind_front с помощью вариабельной шаблонной лямбды. Для этого:
захватите переданный функциональный объект и первые несколько аргументов для него;
напишите аргументы лямбды с помощью auto либо <typename...>;
реализуйте тело лямбды, чтобы она вызывала захваченный функциональный объект с нужными аргументами.

```cpp
template <typename F, typename... Ts>
auto simple_bind_front(F &&func, Ts &&...boundArgs) {
    return [/*...*/](/*...*/) -> decltype(auto) {
        return /*...*/
    };
} 
```

- Используйте идеальную передачу для сохранения функционального объекта и пакета аргументов для него.
- Используйте захват с инициализацией.
- Раскройте пакет сохранённых аргументов при вызове сохранённого функционального объекта.
- Используйте идеальную передачу для аргументов лямбды при вызове сохранённого функционального объекта.