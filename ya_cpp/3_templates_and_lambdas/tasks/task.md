# Задания по шаблонам и лямбдам

## 1. Шаблоны функций

1. Напишите шаблон функции `square`, который возвращает квадрат значения.
2. Напишите шаблон функции `min_value`, который возвращает меньшее из двух значений одного типа.
3. Напишите шаблон функции `add`, который принимает два разных типа и возвращает их сумму.
4. Напишите шаблон функции `print_type_size`, который выводит `sizeof(T)` для переданного значения.
5. Напишите шаблон функции `make_default<T>`, который возвращает значение `T{}`.
6. Напишите шаблон функции `swap_values`, который меняет местами два значения одного типа.
7. Напишите шаблон функции `clamp_value`, который ограничивает значение диапазоном `[low, high]`.
8. Напишите шаблон функции `is_equal`, который сравнивает два значения одного типа.
9. Напишите шаблон функции `apply_twice`, который применяет функцию к значению два раза.
10. Напишите шаблон функции `call_and_print`, который вызывает переданную функцию и печатает результат.

## 2. Перегрузка функций и вывод типов в шаблонах

1. Напишите две перегрузки `print`: для `int` и для `double`.
2. Добавьте шаблонную перегрузку `print` для остальных типов.
3. Проверьте, какая перегрузка вызывается для `int`, `double`, `char`, `std::string`.
4. Напишите шаблон `foo(T value)` и проверьте, что `const int` выводится как `int`.
5. Напишите шаблон `foo(T& value)` и проверьте, что `const int` сохраняет `const`.
6. Напишите шаблон `foo(const T& value)` и проверьте вывод `T` для `int* const`.
7. Напишите шаблон `foo(T* ptr)` и проверьте вывод `T` для `int*` и `const int*`.
8. Напишите функцию `max_value(T a, T b)` и проверьте ошибку для `int` и `double`.
9. Исправьте вызов `max_value(1, 2.5)` явным указанием `T`.
10. Напишите набор перегрузок, где обычная функция выигрывает у шаблона при точном совпадении.

## 3. Шаблон класса и переменной

1. Напишите шаблон класса `Box<T>`, который хранит одно значение.
2. Добавьте в `Box<T>` метод `get`, возвращающий значение.
3. Добавьте в `Box<T>` метод `set`, меняющий значение.
4. Создайте `Box<int>` и `Box<std::string>`.
5. Напишите шаблон класса `Pair<T, U>` для двух значений разных типов.
6. Напишите шаблон переменной `pi<T>`.
7. Используйте `pi<float>` и `pi<double>`.
8. Напишите шаблон класса `Counter<T>` со статическим счетчиком объектов.
9. Проверьте, что `Counter<int>` и `Counter<double>` имеют разные счетчики.
10. Напишите класс с конструктором и проверьте CTAD без явного указания `<T>`.

## 4. Специализация шаблона

1. Напишите шаблон `TypeName<T>`, который возвращает строку `"unknown"`.
2. Сделайте полную специализацию `TypeName<int>`.
3. Сделайте полную специализацию `TypeName<double>`.
4. Напишите шаблон класса `Printer<T>`.
5. Специализируйте `Printer<bool>`, чтобы печатать `true/false`.
6. Напишите шаблон `IsPointer<T>` со значением `false`.
7. Сделайте частичную специализацию `IsPointer<T*>` со значением `true`.
8. Напишите шаблон `RemovePointer<T>`.
9. Сделайте частичную специализацию `RemovePointer<T*>`.
10. Перепишите простую специализацию функции через перегрузку и сравните читаемость.

## 5. Шаблонные параметры, не являющиеся типами

1. Напишите `StaticArray<T, N>` с массивом размера `N`.
2. Добавьте метод `size`, возвращающий `N`.
3. Напишите функцию `repeat<N>`, которая печатает строку `N` раз.
4. Напишите шаблон `LimitChecker<Limit>` с методом `check`.
5. Напишите шаблон `Power<Base, Exp>` для вычисления степени на этапе компиляции.
6. Напишите класс `CallbackHolder<Callback>`, который вызывает функцию `Callback`.
7. Напишите `Matrix<T, Rows, Cols>` с хранением `Rows * Cols` элементов.
8. Напишите шаблон `Wrapper<Container>`, где `Container` - шаблон с одним типовым параметром.
9. Напишите `WrapperByInt<C>`, где `C` - шаблон с нетиповым параметром `int`.
10. Напишите пример, где разные значения нетипового параметра создают несовместимые типы.

## 6. Вещественные числа и пользовательские типы как параметры шаблона

1. Напишите шаблон `EpsilonCompare<T, epsilon>`.
2. Используйте `epsilon` для сравнения двух чисел с плавающей точкой.
3. Напишите пользовательский тип `Dimensions` с `rows` и `cols`.
4. Передайте объект `Dimensions` как нетиповой параметр шаблона.
5. Напишите `Matrix<T, dims>`, где размер берется из `dims.rows * dims.cols`.
6. Добавьте метод `rows`, который возвращает `dims.rows`.
7. Добавьте метод `cols`, который возвращает `dims.cols`.
8. Создайте два разных объекта `Dimensions` и проверьте, что типы матриц разные.
9. Напишите `Threshold<T, value>` для проверки значения относительно порога.
10. Сравните вариант с `template <auto value>` и `template <typename T, T value>`.

## 7. Идеальная передача и auto

1. Напишите функцию `pass`, принимающую `T&&`.
2. Проверьте, как она принимает lvalue и rvalue.
3. Напишите функцию `forward_to`, которая передает аргумент в другую функцию через `std::forward`.
4. Покажите разницу между `std::move` и `std::forward`.
5. Напишите `make_box`, который создает объект `Box<T>` через perfect forwarding.
6. Напишите функцию, где `auto` теряет ссылку.
7. Исправьте ее с помощью `auto&`.
8. Напишите функцию, где `decltype(auto)` сохраняет ссылочный результат.
9. Напишите обертку над функцией, которая возвращает результат через `decltype(auto)`.
10. Напишите `compose`, который хранит две функции и вызывает `f(g(x))`.

## 8. Зависимые типы и member types

1. Напишите функцию, которая использует `typename Container::value_type`.
2. Напишите `print_first`, который принимает контейнер и печатает первый элемент.
3. Напишите `Stack<T, Container>` поверх контейнера.
4. Используйте `Container::value_type` для проверки типа элементов.
5. Добавьте `push`, `pop`, `top` в `Stack`.
6. Напишите шаблонный конструктор `Stack(It first, It last)`.
7. Объясните в коде, почему `It` не должен быть параметром класса.
8. Напишите trait `HasValueType<T>`.
9. Используйте `static_assert`, чтобы проверить совместимость `T` и `Container::value_type`.
10. Напишите обертку, которая использует `Container::reference` и `Container::size_type`.

## 9. Паттерны и идиомы шаблонов

1. Напишите простой trait `IsInt<T>`.
2. Напишите trait `IsSame<T, U>`.
3. Используйте `static_assert` для проверки типа.
4. Напишите функцию с `if constexpr` для разных типов.
5. Напишите `print_value`, который по-разному печатает числа и строки.
6. Напишите tag dispatch для обработки `std::true_type` и `std::false_type`.
7. Напишите compile-time факториал через шаблон.
8. Напишите compile-time сумму чисел от `1` до `N`.
9. Напишите trait `IsContainer<T>` через проверку `value_type`.
10. Перепишите один пример с tag dispatch на `if constexpr`.

## 10. CRTP и статический полиморфизм

1. Напишите базовый класс `Base<Derived>`.
2. Добавьте метод `interface`, который вызывает `Derived::implementation`.
3. Создайте класс `Printer`, наследующийся от `Base<Printer>`.
4. Напишите CRTP-базу `Comparable<Derived>` с оператором `!=` через `==`.
5. Добавьте оператор `>` через `<`.
6. Напишите класс `Point`, использующий `Comparable<Point>`.
7. Напишите CRTP-базу `Counter<Derived>` для подсчета объектов каждого типа.
8. Проверьте, что разные наследники имеют разные счетчики.
9. Напишите статический интерфейс без `virtual`.
10. Сравните пример CRTP с вариантом через виртуальные функции.

## 11. Миксины и явный параметр this

1. Напишите миксин `Printable`, который вызывает `to_string` у наследника.
2. Добавьте миксин `Resettable`, который вызывает `reset`.
3. Создайте класс, наследующийся от двух миксинов.
4. Напишите миксин через CRTP.
5. Добавьте проверку, что наследник имеет нужный метод.
6. Напишите миксин `Addable`, который добавляет `operator+`.
7. Напишите миксин `Incrementable`, который добавляет `operator++`.
8. Напишите пример конфликта имен при нескольких миксинах.
9. Напишите простой метод с явным `this` в C++23.
10. Перепишите пример с явным `this` в обычный метод и сравните форму записи.

## 12. Вариабельные шаблоны и их идиомы

1. Напишите функцию `count_args`, возвращающую `sizeof...(Args)`.
2. Напишите функцию `print_all`, печатающую все аргументы.
3. Напишите `sum_all` через fold expression.
4. Напишите `all_true`, который проверяет все bool-аргументы.
5. Напишите `any_true`, который проверяет хотя бы один bool-аргумент.
6. Напишите `make_vector`, который принимает произвольное количество значений одного типа.
7. Напишите функцию `call`, которая передает пакет аргументов дальше через `std::forward`.
8. Напишите `Logger`, который принимает любое число аргументов и печатает их.
9. Обработайте пустой пакет в `sum_all`.
10. Напишите `compose_many`, который применяет цепочку функций.

## 13. Лямбда-выражения и стандартная библиотека

1. Напишите простую лямбду для возведения числа в квадрат.
2. Напишите лямбду, которая захватывает значение по копии.
3. Напишите лямбду, которая захватывает значение по ссылке.
4. Используйте `std::sort` с лямбдой-компаратором.
5. Используйте `std::find_if` с лямбдой-предикатом.
6. Используйте `std::count_if` для подсчета четных чисел.
7. Напишите лямбду с `mutable`.
8. Покажите пример опасного захвата по ссылке.
9. Напишите функцию, возвращающую лямбду.
10. Напишите обработку списка заказов через несколько STL-алгоритмов и лямбд.

## 14. Шаблонные и вариабельные лямбда-выражения

1. Напишите generic lambda `identity`.
2. Напишите generic lambda `add`.
3. Напишите шаблонную лямбду `same<T>`, принимающую два значения одного типа.
4. Напишите шаблонную лямбду `cast_to<T>`.
5. Напишите вариабельную лямбду `print_all`.
6. Напишите вариабельную лямбду `sum`.
7. Напишите лямбду, которая принимает только интегральные типы.
8. Напишите структуру `Overloaded` для объединения лямбд.
9. Используйте `Overloaded` со `std::variant<int, std::string>`.
10. Напишите обработчик команд через набор перегруженных лямбд.

## 15. SFINAE

1. Напишите функцию, включенную только для integral-типов через `std::enable_if`.
2. Напишите функцию, включенную только для floating-point типов.
3. Сделайте две перегрузки `print_number` для integral и floating-point.
4. Напишите trait `HasSize<T>` через `std::void_t`.
5. Напишите функцию `size_of`, которая работает только если у объекта есть `.size()`.
6. Добавьте запасную перегрузку `size_of` для типов без `.size()`.
7. Напишите trait `HasBeginEnd<T>`.
8. Напишите функцию `is_empty`, доступную только для контейнеров.
9. Покажите пример, где ошибка в теле функции не является SFINAE.
10. Перепишите один SFINAE-пример через `if constexpr` или концепт.

## 16. Ограничения и концепты

1. Напишите функцию с `requires std::integral<T>`.
2. Напишите функцию с сокращенной записью `std::integral auto`.
3. Напишите концепт `Number`.
4. Напишите концепт `HasSize`.
5. Ограничьте функцию `size_of` концептом `HasSize`.
6. Напишите концепт `Addable`, проверяющий `a + b`.
7. Напишите шаблон `sum`, доступный только для `Addable`.
8. Напишите две перегрузки с разными концептами и проверьте выбор лучшей.
9. Перепишите SFINAE-задачу на концепты.
10. Напишите концепт для контейнера с `begin`, `end` и `value_type`.

## 17. constexpr, if constexpr и consteval

1. Напишите `constexpr` функцию `square`.
2. Используйте ее для размера массива.
3. Напишите `constexpr` функцию `factorial`.
4. Напишите функцию с `if constexpr`, которая по-разному обрабатывает числа и строки.
5. Напишите шаблон `type_name`, выбирающий строку через `if constexpr`.
6. Напишите `constexpr` функцию, которую можно вызвать и во время выполнения.
7. Напишите `consteval` функцию `checked_positive`.
8. Проверьте, что `consteval` нельзя вызвать с runtime-значением.
9. Напишите compile-time проверку размера типа через `static_assert`.
10. Напишите функцию `checked_square`, где compile-time и runtime пути отличаются.
