//3. Напишите пользовательский тип `Dimensions` с `rows` и `cols`.
#include <cstddef>
#include <string>


class Dimensions{
    public:
        std::size_t rows_;
        std::size_t cols_;
};

int main() {
    Dimensions d;

    return 0;
}