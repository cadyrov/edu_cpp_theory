# Тест по теме: L-value и R-value в C++

**Всего вопросов: 35** (от простого к сложному)

## Вопрос 1 (Простой)
Что такое L-value в C++?

```cpp
int x = 42;
```

**Варианты ответов:**
A) Значение, которое можно использовать только справа от оператора присваивания
B) Выражение, которое ссылается на объект с определенным адресом памяти
C) Временное значение, которое уничтожается после использования
D) Константное значение, которое нельзя изменить

---

## Вопрос 2 (Простой)
Что такое R-value в C++?

```cpp
int y = 10 + 5;  // 10 + 5 - это?
```

**Варианты ответов:**
A) Выражение с адресом памяти
B) Временное значение или объект, который можно только использовать, но нельзя присвоить
C) Переменная, которая может быть изменена
D) Ссылка на существующий объект

---

## Вопрос 3 (Простой)
Какое из следующих выражений является L-value?

```cpp
int x = 5;
int y = 10;
```

**Варианты ответов:**
A) `x + y`
B) `42`
C) `x`
D) `x + y + 5`

---

## Вопрос 4 (Простой)
Какое из следующих выражений является R-value?

```cpp
int x = 5;
```

**Варианты ответов:**
A) `x`
B) `x = 10`
C) `5`
D) `++x`

---

## Вопрос 5 (Средний)
Что произойдет при компиляции следующего кода?

```cpp
int x = 10;
int& ref = 42;  // Что здесь?
```

**Варианты ответов:**
A) Код скомпилируется успешно
B) Ошибка компиляции: L-value ссылка не может быть привязана к R-value
C) Предупреждение компилятора
D) Неопределенное поведение

---

## Вопрос 6 (Средний)
Что произойдет при компиляции следующего кода?

```cpp
int x = 10;
const int& ref = 42;  // Что здесь?
```

**Варианты ответов:**
A) Ошибка компиляции: нельзя привязать ссылку к литералу
B) Код скомпилируется: const L-value ссылка может привязываться к R-value
C) Предупреждение компилятора
D) Неопределенное поведение

---

## Вопрос 7 (Средний)
Что произойдет при компиляции следующего кода?

```cpp
int x = 10;
int&& ref = x;  // Что здесь?
```

**Варианты ответов:**
A) Код скомпилируется успешно
B) Ошибка компиляции: R-value ссылка не может быть привязана к L-value
C) Предупреждение компилятора
D) Неопределенное поведение

---

## Вопрос 8 (Средний)
Что произойдет при компиляции следующего кода?

```cpp
int x = 10;
int&& ref = std::move(x);  // Что здесь?
```

**Варианты ответов:**
A) Ошибка компиляции
B) Код скомпилируется: std::move конвертирует L-value в R-value
C) Предупреждение компилятора
D) Неопределенное поведение

---

## Вопрос 9 (Средний)
Что делает функция `std::move`?

```cpp
std::string s = "hello";
std::string&& ref = std::move(s);
```

**Варианты ответов:**
A) Перемещает объект из одного места в другое
B) Конвертирует L-value в R-value, но не перемещает сам объект
C) Удаляет объект
D) Копирует объект

---

## Вопрос 10 (Средний)
Что произойдет после выполнения следующего кода?

```cpp
std::string s1 = "hello";
std::string s2 = std::move(s1);
// Что можно сказать о s1?
```

**Варианты ответов:**
A) s1 уничтожен
B) s1 пустой, но валидный
C) s1 содержит "hello"
D) s1 содержит неопределенные данные

---

## Вопрос 11 (Средний)
Какой тип ссылки нужно использовать для приема временных объектов?

```cpp
void process(/* что здесь? */ std::string text) {
    // Принимаем временный объект
}
```

**Варианты ответов:**
A) `const std::string&`
B) `std::string&`
C) `std::string&&`
D) `std::string`

---

## Вопрос 12 (Средний)
Что произойдет при компиляции следующего кода?

```cpp
void func(int& x) {}
void func(int&& x) {}

int main() {
    int y = 5;
    func(y);        // Вызов 1
    func(10);       // Вызов 2
}
```

**Варианты ответов:**
A) Ошибка компиляции: неоднозначный вызов
B) Вызов 1 использует int&, вызов 2 использует int&&
C) Оба вызова используют int&
D) Оба вызова используют int&&

---

## Вопрос 13 (Средний)
Что такое perfect forwarding?

```cpp
template<typename T>
void wrapper(T&& arg) {
    process(std::forward<T>(arg));
}
```

**Варианты ответов:**
A) Механизм для передачи аргументов без изменения их категории значения
B) Механизм для копирования всех аргументов
C) Механизм для перемещения всех аргументов
D) Механизм для оптимизации компилятора

---

## Вопрос 14 (Средний)
Что произойдет при компиляции следующего кода?

```cpp
std::string getName() {
    std::string name = "John";
    return std::move(name);  // Нужно ли?
}
```

**Варианты ответов:**
A) Код правильный, std::move необходим
B) std::move избыточен, RVO (Return Value Optimization) сработает сама
C) Ошибка компиляции
D) Неопределенное поведение

---

## Вопрос 15 (Средний)
Какое выражение является xvalue (eXpiring value)?

```cpp
int x = 10;
```

**Варианты ответов:**
A) `x`
B) `42`
C) `std::move(x)`
D) `x + 5`

---

## Вопрос 16 (Средний)
Можно ли использовать R-value ссылку как поле класса?

```cpp
class Container {
    std::string&& data_;  // Можно ли?
};
```

**Варианты ответов:**
A) Да, это безопасно
B) Нет, это опасно - временный объект может быть уничтожен
C) Можно, но только с const
D) Можно, но только для примитивных типов

---

## Вопрос 17 (Средний)
Что произойдет при компиляции следующего кода?

```cpp
template<typename T>
void func(T&& arg) {}

int x = 5;
func(x);   // Вызов 1
func(10);  // Вызов 2
```

**Варианты ответов:**
A) Ошибка компиляции: неоднозначный вызов
B) Вызов 1: T = int&, вызов 2: T = int
C) Вызов 1: T = int, вызов 2: T = int&&
D) Оба вызова: T = int

---

## Вопрос 18 (Сложный)
Что произойдет при компиляции следующего кода?

```cpp
template<typename T>
void process(T&& arg) {
    T copy = arg;  // Что здесь?
}
```

**Варианты ответов:**
A) Всегда создается копия
B) Если arg - L-value, создается копия; если R-value, создается копия (нужен std::forward)
C) Всегда перемещается
D) Ошибка компиляции

---

## Вопрос 19 (Сложный)
Что такое reference collapsing (коллапсирование ссылок)?

```cpp
template<typename T>
void func(T&& arg) {
    // Что происходит с типом T&&?
}
```

**Варианты ответов:**
A) Механизм для объединения нескольких ссылок в одну
B) Правила преобразования типов при подстановке ссылок в шаблоны
C) Механизм для удаления ссылок из типов
D) Оптимизация компилятора для ссылок

---

## Вопрос 20 (Сложный)
Что будет результатом коллапсирования `T& &&`?

```cpp
// Если T = int&, то T&& = ?
```

**Варианты ответов:**
A) `int&&`
B) `int&`
C) `int`
D) Ошибка компиляции

---

## Вопрос 21 (Сложный)
Что будет результатом коллапсирования `T&& &&`?

```cpp
// Если T = int&&, то T&& = ?
```

**Варианты ответов:**
A) `int&`
B) `int&&`
C) `int`
D) Ошибка компиляции

---

## Вопрос 22 (Сложный)
Как правильно реализовать perfect forwarding?

```cpp
template<typename T>
void wrapper(T&& arg) {
    // Как правильно передать arg дальше?
    process(/* что здесь? */);
}
```

**Варианты ответов:**
A) `process(arg);`
B) `process(std::move(arg));`
C) `process(std::forward<T>(arg));`
D) `process(static_cast<T&&>(arg));`

---

## Вопрос 23 (Сложный)
Что произойдет при выполнении следующего кода?

```cpp
std::string createString() {
    return "hello";
}

void process(const std::string& s) {
    std::cout << s;
}

int main() {
    process(createString());  // Что здесь?
}
```

**Варианты ответов:**
A) Ошибка компиляции: нельзя передать временный объект
B) Временный объект будет уничтожен до вызова process
C) Временный объект живет до конца выражения, код работает корректно
D) Неопределенное поведение

---

## Вопрос 24 (Сложный)
Что произойдет при компиляции следующего кода?

```cpp
class Container {
public:
    void add(const std::string& s) { /* копирование */ }
    void add(std::string&& s) { /* перемещение */ }
};

int main() {
    Container c;
    std::string s = "test";
    c.add(s);           // Вызов 1
    c.add("temporary"); // Вызов 2
}
```

**Варианты ответов:**
A) Ошибка компиляции: неоднозначный вызов
B) Вызов 1: const std::string&, вызов 2: std::string&&
C) Оба вызова используют const std::string&
D) Оба вызова используют std::string&&

---

## Вопрос 25 (Сложный)
Что произойдет при компиляции следующего кода?

```cpp
int& getRef() {
    int x = 10;
    return x;  // Что здесь?
}
```

**Варианты ответов:**
A) Код скомпилируется и будет работать корректно
B) Ошибка компиляции: нельзя вернуть ссылку на локальную переменную
C) Код скомпилируется, но будет неопределенное поведение
D) Предупреждение компилятора

---

## Вопрос 26 (Сложный)
Что произойдет при компиляции следующего кода?

```cpp
int&& getRRef() {
    int x = 10;
    return std::move(x);  // Что здесь?
}
```

**Варианты ответов:**
A) Код правильный, x будет перемещен
B) Ошибка компиляции: нельзя вернуть R-value ссылку на локальную переменную
C) Код скомпилируется, но будет неопределенное поведение
D) Предупреждение компилятора

---

## Вопрос 27 (Сложный)
Что произойдет при выполнении следующего кода?

```cpp
std::string s1 = "hello";
std::string s2 = s1;           // Операция 1
std::string s3 = std::move(s1); // Операция 2
// Что можно сказать о s1, s2, s3?
```

**Варианты ответов:**
A) s1 пустой, s2 = "hello", s3 = "hello"
B) s1 = "hello", s2 = "hello", s3 = "hello"
C) s1 уничтожен, s2 = "hello", s3 = "hello"
D) Все строки пустые

---

## Вопрос 28 (Сложный)
Какой тип будет у переменной `x` в следующем коде?

```cpp
int value = 42;
auto&& x = value;  // Какой тип у x?
```

**Варианты ответов:**
A) `int&&`
B) `int&`
C) `int`
D) `const int&`

---

## Вопрос 29 (Сложный)
Какой тип будет у переменной `y` в следующем коде?

```cpp
auto&& y = 42;  // Какой тип у y?
```

**Варианты ответов:**
A) `int&`
B) `int&&`
C) `int`
D) `const int&`

---

## Вопрос 30 (Сложный)
Что произойдет при компиляции следующего кода?

```cpp
void process(int& x) { std::cout << "lvalue\n"; }
void process(int&& x) { std::cout << "rvalue\n"; }

template<typename T>
void wrapper(T&& arg) {
    process(arg);  // Что будет выведено?
}

int main() {
    int x = 5;
    wrapper(x);    // Вызов 1
    wrapper(10);   // Вызов 2
}
```

**Варианты ответов:**
A) "lvalue" дважды (arg всегда L-value в теле функции)
B) "lvalue", затем "rvalue"
C) "rvalue" дважды
D) Ошибка компиляции: неоднозначный вызов

---

## Вопрос 31 (Сложный)
Что произойдет при компиляции следующего кода?

```cpp
void process(int& x) { std::cout << "lvalue\n"; }
void process(int&& x) { std::cout << "rvalue\n"; }

template<typename T>
void wrapper(T&& arg) {
    process(std::forward<T>(arg));  // Что будет выведено?
}

int main() {
    int x = 5;
    wrapper(x);    // Вызов 1
    wrapper(10);   // Вызов 2
}
```

**Варианты ответов:**
A) "lvalue" дважды
B) "lvalue", затем "rvalue"
C) "rvalue" дважды
D) Ошибка компиляции

---

## Вопрос 32 (Сложный)
Что произойдет при компиляции следующего кода?

```cpp
class Test {
public:
    Test() = default;
    Test(const Test&) { std::cout << "copy\n"; }
    Test(Test&&) { std::cout << "move\n"; }
};

Test create() {
    Test t;
    return t;  // Что здесь?
}
```

**Варианты ответов:**
A) Всегда вызывается конструктор копирования
B) Всегда вызывается конструктор перемещения
C) Может быть RVO (Return Value Optimization), конструктор не вызывается
D) Неопределенное поведение

---

## Вопрос 33 (Сложный)
Что произойдет при компиляции следующего кода?

```cpp
class Test {
public:
    Test() = default;
    Test(const Test&) { std::cout << "copy\n"; }
    Test(Test&&) { std::cout << "move\n"; }
};

Test create() {
    Test t;
    return std::move(t);  // Нужно ли std::move?
}
```

**Варианты ответов:**
A) std::move необходим для перемещения
B) std::move избыточен, может помешать RVO
C) Ошибка компиляции
D) Неопределенное поведение

---

## Вопрос 34 (Сложный)
Что произойдет при компиляции следующего кода?

```cpp
template<typename T>
void func(T param) {
    // param - какой категории значения?
}

int x = 5;
func(x);   // Вызов 1
func(10);  // Вызов 2
```

**Варианты ответов:**
A) В обоих случаях param - L-value
B) В обоих случаях param - R-value
C) Вызов 1: L-value, вызов 2: R-value
D) Зависит от типа T

---

## Вопрос 35 (Сложный)
Что произойдет при компиляции следующего кода?

```cpp
template<typename T>
void func(T&& param) {
    T copy = param;  // Что здесь?
    T move = std::move(param);  // А здесь?
}

int x = 5;
func(x);   // Вызов 1
func(10);  // Вызов 2
```

**Варианты ответов:**
A) В обоих случаях copy - копия, move - перемещение
B) Вызов 1: copy - копия, move - перемещение; Вызов 2: copy - копия, move - перемещение
C) Вызов 1: copy - ссылка (T = int&), move - ошибка; Вызов 2: copy - копия, move - перемещение
D) Ошибка компиляции

---

## Правильные ответы

1. **B** - Выражение, которое ссылается на объект с определенным адресом памяти
2. **B** - Временное значение или объект, который можно только использовать, но нельзя присвоить
3. **C** - `x`
4. **C** - `5`
5. **B** - Ошибка компиляции: L-value ссылка не может быть привязана к R-value
6. **B** - Код скомпилируется: const L-value ссылка может привязываться к R-value
7. **B** - Ошибка компиляции: R-value ссылка не может быть привязана к L-value
8. **B** - Код скомпилируется: std::move конвертирует L-value в R-value
9. **B** - Конвертирует L-value в R-value, но не перемещает сам объект
10. **B** - s1 пустой, но валидный
11. **C** - `std::string&&`
12. **B** - Вызов 1 использует int&, вызов 2 использует int&&
13. **A** - Механизм для передачи аргументов без изменения их категории значения
14. **B** - std::move избыточен, RVO (Return Value Optimization) сработает сама
15. **C** - `std::move(x)`
16. **B** - Нет, это опасно - временный объект может быть уничтожен
17. **B** - Вызов 1: T = int&, вызов 2: T = int
18. **B** - Если arg - L-value, создается копия; если R-value, создается копия (нужен std::forward)
19. **B** - Правила преобразования типов при подстановке ссылок в шаблоны
20. **B** - `int&`
21. **B** - `int&&`
22. **C** - `process(std::forward<T>(arg));`
23. **C** - Временный объект живет до конца выражения, код работает корректно
24. **B** - Вызов 1: const std::string&, вызов 2: std::string&&
25. **C** - Код скомпилируется, но будет неопределенное поведение
26. **C** - Код скомпилируется, но будет неопределенное поведение
27. **A** - s1 пустой, s2 = "hello", s3 = "hello"
28. **B** - `int&`
29. **B** - `int&&`
30. **A** - "lvalue" дважды (arg всегда L-value в теле функции)
31. **B** - "lvalue", затем "rvalue"
32. **C** - Может быть RVO (Return Value Optimization), конструктор не вызывается
33. **B** - std::move избыточен, может помешать RVO
34. **A** - В обоих случаях param - L-value
35. **C** - Вызов 1: copy - ссылка (T = int&), move - ошибка; Вызов 2: copy - копия, move - перемещение







