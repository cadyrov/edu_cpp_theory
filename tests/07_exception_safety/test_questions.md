# Тест по теме: Exception Safety (Безопасность исключений) в C++

**Всего вопросов: 35** (от простого к сложному)

## Вопрос 1 (Простой)
Что такое Exception Safety?

```cpp
void func() {
    // Код, который может бросить исключение
}
```

**Варианты ответов:**
A) Механизм для предотвращения всех исключений
B) Набор гарантий, которые код предоставляет при возникновении исключений
C) Способ полностью игнорировать исключения
D) Механизм для автоматического восстановления после исключений

---

## Вопрос 2 (Простой)
Сколько уровней гарантий Exception Safety существует?

```cpp
// Какие гарантии могут быть предоставлены?
```

**Варианты ответов:**
A) Один уровень
B) Два уровня
C) Три уровня (базовая, строгая, no-throw)
D) Четыре уровня

---

## Вопрос 3 (Простой)
Что такое базовая гарантия (Basic Guarantee)?

```cpp
void basic() {
    std::vector<std::string> vec;
    vec.push_back("data");  // Может бросить исключение
}
```

**Варианты ответов:**
A) Гарантия, что исключения не возникнут
B) Гарантия отсутствия утечек ресурсов и сохранения инвариантов
C) Гарантия, что состояние объекта не изменится
D) Гарантия, что программа не завершится

---

## Вопрос 4 (Простой)
Что произойдет с ресурсом при исключении в следующем коде?

```cpp
void leak() {
    Resource* res = new Resource();
    doSomething();  // Может бросить исключение
    delete res;     // Что если исключение произойдет выше?
}
```

**Варианты ответов:**
A) Ресурс автоматически освободится
B) Утечка ресурса, если исключение произойдет до delete
C) Ошибка компиляции
D) Неопределенное поведение

---

## Вопрос 5 (Средний)
Что такое строгая гарантия (Strong Guarantee)?

```cpp
class Atomic {
    void update() {
        auto temp = *this;  // Копия
        // Изменения...
        *this = std::move(temp);  // Атомарная замена
    }
};
```

**Варианты ответов:**
A) Гарантия отсутствия утечек
B) Гарантия, что состояние объекта не изменится при исключении (транзакционность)
C) Гарантия отсутствия исключений
D) Гарантия быстрого выполнения

---

## Вопрос 6 (Средний)
Что такое no-throw гарантия?

```cpp
void noexcept_func() noexcept {
    // Что гарантирует noexcept?
}
```

**Варианты ответов:**
A) Гарантия отсутствия утечек
B) Гарантия, что функция не бросит исключение
C) Гарантия сохранения состояния
D) Гарантия быстрого выполнения

---

## Вопрос 7 (Средний)
Как RAII помогает обеспечить exception safety?

```cpp
void safe() {
    std::lock_guard<std::mutex> lock(mutex_);
    // Что если здесь будет исключение?
}
```

**Варианты ответов:**
A) RAII не помогает с исключениями
B) Ресурсы автоматически освободятся при раскрутке стека
C) Исключения не могут возникнуть в RAII-коде
D) RAII замедляет обработку исключений

---

## Вопрос 8 (Средний)
Что произойдет при выполнении следующего кода?

```cpp
class Broken {
    std::string name_;
    std::vector<int> data_;
public:
    void update(const std::string& name, const std::vector<int>& data) {
        name_ = name;   // Если тут исключение
        data_ = data;   // Объект в неконсистентном состоянии
    }
};
```

**Варианты ответов:**
A) Код безопасен, объект всегда консистентен
B) Нарушение инвариантов при исключении в name_
C) Ошибка компиляции
D) Неопределенное поведение

---

## Вопрос 9 (Средний)
Как правильно обеспечить строгую гарантию при обновлении объекта?

```cpp
class Safe {
    std::string name_;
    std::vector<int> data_;
public:
    void update(const std::string& name, const std::vector<int>& data) {
        // Как обеспечить строгую гарантию?
    }
};
```

**Варианты ответов:**
A) Обновлять поля последовательно
B) Создать копию состояния, обновить копию, затем атомарно заменить
C) Использовать только один оператор присваивания
D) Проверять все значения перед обновлением

---

## Вопрос 10 (Средний)
Что произойдет при компиляции следующего кода?

```cpp
void silentFail() {
    try {
        riskyOperation();
    } catch (...) {
        // Проглатывание исключения
    }
}
```

**Варианты ответов:**
A) Код правильный, исключение обработано
B) Проглатывание исключения без обработки - плохая практика
C) Ошибка компиляции
D) Неопределенное поведение

---

## Вопрос 11 (Средний)
Как правильно обработать исключение, сохранив его для последующей обработки?

```cpp
// Как сохранить исключение?
```

**Варианты ответов:**
A) `std::exception_ptr eptr = std::current_exception();`
B) `std::exception e = std::current_exception();`
C) `throw;` (перебросить сразу)
D) Нельзя сохранить исключение

---

## Вопрос 12 (Средний)
Что произойдет при выполнении следующего кода?

```cpp
class Resource {
    std::unique_ptr<Data> data_;
public:
    void process() {
        data_ = std::make_unique<Data>();
        data_->doWork();  // Может бросить исключение
        // Что с памятью при исключении?
    }
};
```

**Варианты ответов:**
A) Утечка памяти
B) Память автоматически освободится через unique_ptr
C) Ошибка компиляции
D) Неопределенное поведение

---

## Вопрос 13 (Средний)
Как правильно реализовать транзакционное поведение?

```cpp
template<typename F>
void transaction(F&& func) {
    auto backup = state_;
    try {
        func();
    } catch (...) {
        // Что здесь?
    }
}
```

**Варианты ответов:**
A) `state_ = backup; throw;` (восстановить и перебросить)
B) `state_ = backup;` (только восстановить)
C) `throw;` (только перебросить)
D) Ничего не делать

---

## Вопрос 14 (Средний)
Что произойдет при компиляции следующего кода?

```cpp
class Safe {
    ~Safe() {
        try {
            cleanup();
        } catch (...) {
            // Что здесь?
        }
    }
};
```

**Варианты ответов:**
A) Код правильный, исключения обработаны
B) Деструкторы должны быть noexcept, исключения нужно логировать
C) Ошибка компиляции
D) Неопределенное поведение

---

## Вопрос 15 (Средний)
Как правильно пометить функцию как не бросающую исключений?

```cpp
void safeFunction() {
    // Как пометить функцию?
}
```

**Варианты ответов:**
A) `void safeFunction() noexcept;`
B) `void safeFunction() throw();`
C) `void safeFunction() noexcept(true);`
D) Нельзя гарантировать отсутствие исключений

---

## Вопрос 16 (Сложный)
Что произойдет при выполнении следующего кода?

```cpp
class BankAccount {
    std::string name_;
    double balance_;
public:
    void transfer(BankAccount& to, double amount) {
        balance_ -= amount;  // Если здесь исключение?
        to.balance_ += amount;
    }
};
```

**Варианты ответов:**
A) Код безопасен, состояние всегда консистентно
B) Нарушение строгой гарантии: состояние может быть неконсистентным
C) Ошибка компиляции
D) Неопределенное поведение

---

## Вопрос 17 (Сложный)
Как правильно обеспечить строгую гарантию при transfer?

```cpp
class BankAccount {
    std::string name_;
    double balance_;
public:
    void transfer(BankAccount& to, double amount) {
        // Как обеспечить строгую гарантию?
    }
};
```

**Варианты ответов:**
A) Выполнять операции последовательно
B) Сохранить старое состояние, выполнить операции в try-catch, восстановить при ошибке
C) Использовать только одну операцию
D) Проверять баланс перед операцией

---

## Вопрос 18 (Сложный)
Что произойдет при компиляции следующего кода?

```cpp
void func() noexcept {
    throw std::runtime_error("error");  // Что здесь?
}
```

**Варианты ответов:**
A) Код скомпилируется, исключение будет обработано
B) Код скомпилируется, но вызовется std::terminate()
C) Ошибка компиляции
D) Неопределенное поведение

---

## Вопрос 19 (Сложный)
Как правильно использовать std::exception_ptr?

```cpp
std::exception_ptr eptr;
try {
    riskyCode();
} catch (...) {
    // Как сохранить исключение?
}
// Как позже перебросить?
```

**Варианты ответов:**
A) `eptr = std::current_exception();` и `std::rethrow_exception(eptr);`
B) `eptr = std::exception();` и `throw eptr;`
C) `eptr = std::current_exception();` и `throw eptr;`
D) Нельзя сохранить и перебросить исключение

---

## Вопрос 20 (Сложный)
Что произойдет при выполнении следующего кода?

```cpp
class Container {
    std::vector<std::string> data_;
public:
    void add(const std::string& s) {
        data_.push_back(s);  // Может бросить исключение
        // Какая гарантия?
    }
};
```

**Варианты ответов:**
A) Строгая гарантия
B) Базовая гарантия (нет утечек, инварианты сохранены)
C) No-throw гарантия
D) Никакой гарантии

---

## Вопрос 21 (Сложный)
Как правильно реализовать swap с no-throw гарантией?

```cpp
class NoThrow {
    int data_;
public:
    void swap(NoThrow& other) {
        // Как реализовать с noexcept?
    }
};
```

**Варианты ответов:**
A) `void swap(NoThrow& other) noexcept { using std::swap; swap(data_, other.data_); }`
B) `void swap(NoThrow& other) { data_ = other.data_; }`
C) `void swap(NoThrow& other) noexcept { data_ = other.data_; }`
D) Нельзя сделать swap noexcept

---

## Вопрос 22 (Сложный)
Что произойдет при компиляции следующего кода?

```cpp
class Resource {
    std::unique_ptr<Data> data_;
public:
    void update() {
        auto new_data = std::make_unique<Data>();
        data_ = std::move(new_data);  // Может ли быть исключение?
    }
};
```

**Варианты ответов:**
A) Исключение невозможно
B) Может быть исключение при перемещении, но старая память уже освобождена
C) Может быть исключение, но состояние консистентно
D) Неопределенное поведение

---

## Вопрос 23 (Сложный)
Как правильно обеспечить exception safety при работе с несколькими ресурсами?

```cpp
void multiResource() {
    Resource1* r1 = new Resource1();
    Resource2* r2 = new Resource2();
    // Что если исключение при создании r2?
    // Как правильно?
}
```

**Варианты ответов:**
A) Использовать RAII (smart pointers)
B) Использовать try-catch для каждого ресурса
C) Проверять каждый ресурс перед использованием
D) Использовать только один ресурс

---

## Вопрос 24 (Сложный)
Что произойдет при выполнении следующего кода?

```cpp
class Safe {
    std::string name_;
    std::vector<int> data_;
public:
    void update(const std::string& name, const std::vector<int>& data) {
        std::string old_name = name_;
        std::vector<int> old_data = data_;
        
        try {
            name_ = name;
            data_ = data;
        } catch (...) {
            name_ = old_name;
            data_ = old_data;
            throw;
        }
    }
};
```

**Варианты ответов:**
A) Код обеспечивает строгую гарантию
B) Код обеспечивает только базовую гарантию
C) Код небезопасен
D) Ошибка компиляции

---

## Вопрос 25 (Сложный)
Как правильно обработать исключение в деструкторе?

```cpp
class Safe {
    ~Safe() {
        // Как правильно обработать исключения?
    }
};
```

**Варианты ответов:**
A) `~Safe() noexcept { try { cleanup(); } catch (...) { /* логировать */ } }`
B) `~Safe() { cleanup(); }` (бросить исключение)
C) `~Safe() { try { cleanup(); } catch (...) { throw; } }`
D) Деструкторы не могут бросать исключения

---

## Вопрос 26 (Сложный)
Что произойдет при компиляции следующего кода?

```cpp
void func() {
    std::vector<int> vec;
    vec.reserve(1000);
    for (int i = 0; i < 1000; ++i) {
        vec.push_back(i);  // Может бросить исключение
        // Что с уже добавленными элементами?
    }
}
```

**Варианты ответов:**
A) Все элементы будут потеряны при исключении
B) Уже добавленные элементы сохранятся (базовая гарантия)
C) Ошибка компиляции
D) Неопределенное поведение

---

## Вопрос 27 (Сложный)
Как правильно реализовать copy-and-swap идиому для строгой гарантии?

```cpp
class Container {
    std::vector<int> data_;
public:
    Container& operator=(const Container& other) {
        // Как реализовать copy-and-swap?
    }
};
```

**Варианты ответов:**
A) `Container temp(other); swap(temp); return *this;`
B) `data_ = other.data_; return *this;`
C) `swap(Container(other)); return *this;`
D) `*this = other; return *this;`

---

## Вопрос 28 (Сложный)
Что произойдет при выполнении следующего кода?

```cpp
class Resource {
    std::unique_ptr<Data> data_;
public:
    void replace(Data* new_data) {
        data_.reset(new_data);  // Может ли быть исключение?
    }
};
```

**Варианты ответов:**
A) Исключение невозможно
B) Может быть исключение, но старая память уже освобождена
C) Может быть исключение, состояние консистентно
D) Неопределенное поведение

---

## Вопрос 29 (Сложный)
Как правильно обеспечить exception safety при работе с файлами?

```cpp
void processFile(const char* filename) {
    FILE* file = fopen(filename, "r");
    // Обработка файла...
    fclose(file);  // Что если исключение выше?
}
```

**Варианты ответов:**
A) Использовать RAII-обёртку для файла
B) Использовать try-catch вокруг всего кода
C) Проверять файл перед закрытием
D) Не закрывать файл при исключении

---

## Вопрос 30 (Сложный)
Что произойдет при компиляции следующего кода?

```cpp
class Atomic {
    std::string name_;
    int value_;
public:
    void update(const std::string& name, int val) {
        name_ = name;
        value_ = val;  // Что если исключение в name_?
    }
};
```

**Варианты ответов:**
A) Код безопасен, состояние всегда консистентно
B) Нарушение строгой гарантии: value_ может быть обновлен, а name_ нет
C) Ошибка компиляции
D) Неопределенное поведение

---

## Вопрос 31 (Сложный)
Как правильно использовать std::uncaught_exceptions()?

```cpp
int count = std::uncaught_exceptions();
// Что возвращает эта функция?
```

**Варианты ответов:**
A) Количество необработанных исключений в текущем потоке
B) Количество всех исключений в программе
C) Количество обработанных исключений
D) Эта функция не существует

---

## Вопрос 32 (Сложный)
Что произойдет при выполнении следующего кода?

```cpp
class Safe {
    std::unique_ptr<Resource> res_;
public:
    void update() {
        res_ = std::make_unique<Resource>();
        res_->initialize();  // Может бросить исключение
        // Что с res_ при исключении?
    }
};
```

**Варианты ответов:**
A) res_ будет nullptr
B) res_ будет указывать на неинициализированный Resource
C) res_ будет указывать на корректный Resource (базовая гарантия)
D) Неопределенное поведение

---

## Вопрос 33 (Сложный)
Как правильно реализовать strong exception safety для оператора присваивания?

```cpp
class Container {
    std::vector<int> data_;
public:
    Container& operator=(const Container& other) {
        // Как обеспечить строгую гарантию?
    }
};
```

**Варианты ответов:**
A) `data_ = other.data_; return *this;`
B) `Container temp(other); swap(temp); return *this;` (copy-and-swap)
C) `clear(); data_ = other.data_; return *this;`
D) `*this = other; return *this;`

---

## Вопрос 34 (Сложный)
Что произойдет при компиляции следующего кода?

```cpp
void func() noexcept(noexcept(riskyOperation())) {
    riskyOperation();  // Что означает noexcept(noexcept(...))?
}
```

**Варианты ответов:**
A) Ошибка компиляции: неправильный синтаксис
B) Условный noexcept: функция noexcept только если riskyOperation() noexcept
C) Функция всегда noexcept
D) Функция никогда не noexcept

---

## Вопрос 35 (Сложный)
Как правильно обеспечить exception safety при работе с контейнерами?

```cpp
class Container {
    std::vector<std::unique_ptr<Resource>> resources_;
public:
    void addResource(std::unique_ptr<Resource> res) {
        resources_.push_back(std::move(res));  // Может бросить исключение
        // Что с res при исключении?
    }
};
```

**Варианты ответов:**
A) res будет потерян
B) res останется валидным (базовая гарантия vector)
C) Неопределенное поведение
D) Ошибка компиляции

---

## Правильные ответы

1. **B** - Набор гарантий, которые код предоставляет при возникновении исключений
2. **C** - Три уровня (базовая, строгая, no-throw)
3. **B** - Гарантия отсутствия утечек ресурсов и сохранения инвариантов
4. **B** - Утечка ресурса, если исключение произойдет до delete
5. **B** - Гарантия, что состояние объекта не изменится при исключении (транзакционность)
6. **B** - Гарантия, что функция не бросит исключение
7. **B** - Ресурсы автоматически освободятся при раскрутке стека
8. **B** - Нарушение инвариантов при исключении в name_
9. **B** - Создать копию состояния, обновить копию, затем атомарно заменить
10. **B** - Проглатывание исключения без обработки - плохая практика
11. **A** - `std::exception_ptr eptr = std::current_exception();`
12. **B** - Память автоматически освободится через unique_ptr
13. **A** - `state_ = backup; throw;` (восстановить и перебросить)
14. **B** - Деструкторы должны быть noexcept, исключения нужно логировать
15. **A** - `void safeFunction() noexcept;`
16. **B** - Нарушение строгой гарантии: состояние может быть неконсистентным
17. **B** - Сохранить старое состояние, выполнить операции в try-catch, восстановить при ошибке
18. **B** - Код скомпилируется, но вызовется std::terminate()
19. **A** - `eptr = std::current_exception();` и `std::rethrow_exception(eptr);`
20. **B** - Базовая гарантия (нет утечек, инварианты сохранены)
21. **A** - `void swap(NoThrow& other) noexcept { using std::swap; swap(data_, other.data_); }`
22. **C** - Может быть исключение, но состояние консистентно
23. **A** - Использовать RAII (smart pointers)
24. **A** - Код обеспечивает строгую гарантию
25. **A** - `~Safe() noexcept { try { cleanup(); } catch (...) { /* логировать */ } }`
26. **B** - Уже добавленные элементы сохранятся (базовая гарантия)
27. **A** - `Container temp(other); swap(temp); return *this;`
28. **C** - Может быть исключение, состояние консистентно
29. **A** - Использовать RAII-обёртку для файла
30. **B** - Нарушение строгой гарантии: value_ может быть обновлен, а name_ нет
31. **A** - Количество необработанных исключений в текущем потоке
32. **C** - res_ будет указывать на корректный Resource (базовая гарантия)
33. **B** - `Container temp(other); swap(temp); return *this;` (copy-and-swap)
34. **B** - Условный noexcept: функция noexcept только если riskyOperation() noexcept
35. **B** - res останется валидным (базовая гарантия vector)







