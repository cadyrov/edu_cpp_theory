# Тест по теме: Константность (const) в C++

**Всего вопросов: 32** (от простого к сложному)

## Вопрос 1 (Простой)
Что означает ключевое слово `const` в C++?

```cpp
const int x = 10;
```

**Варианты ответов:**
A) Переменная может быть изменена только один раз
B) Значение переменной не может быть изменено после инициализации
C) Переменная должна быть инициализирована при объявлении
D) Переменная доступна только для чтения в текущей функции

---

## Вопрос 2 (Простой)
Какой из следующих методов класса должен быть помечен как `const`?

```cpp
class BankAccount {
    double balance_;
public:
    // Какой метод должен быть const?
    double getBalance() { return balance_; }
};
```

**Варианты ответов:**
A) `getBalance()` уже правильный, const не нужен
B) `double getBalance() const { return balance_; }`
C) `const double getBalance() { return balance_; }`
D) `double const getBalance() { return balance_; }`

---

## Вопрос 3 (Простой)
Что произойдет при компиляции следующего кода?

```cpp
const int x = 5;
x = 10;
```

**Варианты ответов:**
A) Код скомпилируется и выполнится успешно
B) Ошибка компиляции: нельзя изменять константную переменную
C) Предупреждение компилятора, но код выполнится
D) Неопределенное поведение во время выполнения

---

## Вопрос 4 (Простой)
Как правильно передать объект в функцию, если мы не планируем его изменять?

```cpp
void processUser(/* что здесь? */ User& user) {
    std::cout << user.getName();
}
```

**Варианты ответов:**
A) `User& user`
B) `const User& user`
C) `User user`
D) `User* user`

---

## Вопрос 5 (Средний)
В чем разница между `const int*` и `int* const`?

```cpp
int x = 5;
const int* p1 = &x;  // Вариант 1
int* const p2 = &x;  // Вариант 2
```

**Варианты ответов:**
A) Нет разницы, оба означают константный указатель
B) `const int*` - указатель на константные данные, `int* const` - константный указатель
C) `const int*` - константный указатель, `int* const` - указатель на константные данные
D) Оба варианта неверны, правильный синтаксис `int const*`

---

## Вопрос 6 (Средний)
Что произойдет при выполнении следующего кода?

```cpp
int x = 10;
const int* p = &x;
*p = 20;  // Что здесь?
```

**Варианты ответов:**
A) Код скомпилируется и x станет равным 20
B) Ошибка компиляции: нельзя изменять данные через const указатель
C) Предупреждение, но код выполнится
D) Неопределенное поведение

---

## Вопрос 7 (Средний)
Какой код правильный для константного метода класса?

```cpp
class Counter {
    int count_;
public:
    // Какой вариант правильный?
    void increment() const { count_++; }
    // или
    int getCount() const { return count_; }
};
```

**Варианты ответов:**
A) Оба варианта правильные
B) Только `increment()` правильный
C) Только `getCount()` правильный
D) Оба варианта неправильные

---

## Вопрос 8 (Средний)
Что означает `mutable` в следующем коде?

```cpp
class Cache {
    mutable std::map<std::string, int> cache_;
public:
    int getValue(const std::string& key) const {
        cache_[key] = 42;  // Можно ли так?
        return cache_[key];
    }
};
```

**Варианты ответов:**
A) `mutable` позволяет изменять член класса в константном методе
B) `mutable` запрещает изменение члена класса
C) `mutable` делает член класса константным
D) `mutable` не имеет смысла в этом контексте

---

## Вопрос 9 (Средний)
Какой из следующих операторов должен быть перегружен в константной и неконстантной версиях?

```cpp
class Array {
    int* data_;
public:
    // Какой оператор нужен в двух версиях?
    int& operator[](size_t index) { return data_[index]; }
    const int& operator[](size_t index) const { return data_[index]; }
};
```

**Варианты ответов:**
A) `operator+`
B) `operator[]`
C) `operator=`
D) `operator==`

---

## Вопрос 10 (Средний)
Что произойдет при компиляции следующего кода?

```cpp
class Data {
    int value_;
public:
    void setValue(int v) const {
        value_ = v;
    }
};
```

**Варианты ответов:**
A) Код скомпилируется успешно
B) Ошибка компиляции: нельзя изменять не-mutable члены в const методе
C) Предупреждение, но код выполнится
D) Неопределенное поведение

---

## Вопрос 11 (Сложный)
Что произойдет при выполнении следующего кода?

```cpp
int x = 5;
const int* px = &x;
int* y = const_cast<int*>(px);
*y = 10;
std::cout << x;
```

**Варианты ответов:**
A) Выведет 5
B) Выведет 10
C) Ошибка компиляции
D) Неопределенное поведение

---

## Вопрос 12 (Сложный)
Что произойдет при выполнении следующего кода?

```cpp
const int x = 5;
int* y = const_cast<int*>(&x);
*y = 10;
std::cout << x;
```

**Варианты ответов:**
A) Выведет 5
B) Выведет 10
C) Ошибка компиляции
D) Неопределенное поведение (UB)

---

## Вопрос 13 (Сложный)
Какой код правильный для возврата константной ссылки на внутренний член?

```cpp
class Container {
    std::vector<int> data_;
public:
    // Какой вариант правильный?
    const std::vector<int>& getData() const { return data_; }
    // или
    std::vector<int> getData() const { return data_; }
};
```

**Варианты ответов:**
A) Оба варианта правильные и эквивалентны
B) Только первый вариант правильный (возврат константной ссылки)
C) Только второй вариант правильный (возврат по значению)
D) Оба варианта неправильные

---

## Вопрос 14 (Сложный)
Что произойдет при компиляции следующего кода?

```cpp
class Base {
public:
    virtual void func() const = 0;
};

class Derived : public Base {
public:
    void func() {  // Забыли const
        // ...
    }
};
```

**Варианты ответов:**
A) Код скомпилируется успешно
B) Ошибка компиляции: сигнатуры методов не совпадают
C) Предупреждение о несоответствии сигнатур
D) Неопределенное поведение

---

## Вопрос 15 (Сложный)
Как правильно реализовать константные и неконстантные итераторы?

```cpp
class Container {
    std::vector<int> data_;
public:
    // Что нужно добавить?
    iterator begin() { return data_.begin(); }
    iterator end() { return data_.end(); }
};
```

**Варианты ответов:**
A) Добавить `const_iterator begin() const` и `const_iterator end() const`
B) Добавить `const iterator begin() const` и `const iterator end() const`
C) Итераторы уже правильные, ничего добавлять не нужно
D) Нужно использовать `cbegin()` и `cend()` вместо `begin()` и `end()`

---

## Вопрос 16 (Сложный)
Что произойдет при выполнении следующего кода?

```cpp
class Test {
    mutable int counter_;
public:
    Test() : counter_(0) {}
    void increment() const {
        counter_++;
    }
    int getCounter() const {
        return counter_;
    }
};

const Test t;
t.increment();
std::cout << t.getCounter();
```

**Варианты ответов:**
A) Ошибка компиляции: нельзя вызывать методы у const объекта
B) Ошибка компиляции: нельзя изменять mutable член в const методе
C) Код скомпилируется и выведет 1
D) Неопределенное поведение

---

## Вопрос 17 (Сложный)
Какой из следующих операторов должен возвращать константную ссылку?

```cpp
class Vector {
    double x_, y_;
public:
    // Какой оператор правильный?
    Vector operator+(const Vector& other) const {
        return Vector(x_ + other.x_, y_ + other.y_);
    }
    
    Vector& operator+=(const Vector& other) {
        x_ += other.x_;
        y_ += other.y_;
        return *this;
    }
};
```

**Варианты ответов:**
A) `operator+` должен возвращать `const Vector&`
B) `operator+=` должен возвращать `const Vector&`
C) Оба оператора правильные как есть
D) Оба оператора должны возвращать `const Vector&`

---

## Вопрос 18 (Сложный)
Что произойдет при компиляции следующего кода?

```cpp
class Data {
    int value_;
public:
    Data(int v) : value_(v) {}
    Data(const Data& other) : value_(other.value_) {}
    
    Data& operator=(const Data& other) {
        if (this != &other) {
            value_ = other.value_;
        }
        return *this;
    }
};

const Data d1(10);
const Data d2(20);
d1 = d2;  // Что здесь?
```

**Варианты ответов:**
A) Код скомпилируется и выполнится успешно
B) Ошибка компиляции: нельзя присваивать константному объекту
C) Предупреждение, но код выполнится
D) Неопределенное поведение

---

## Вопрос 19 (Сложный)
Как правильно использовать `const_cast` для безопасного преобразования?

```cpp
void legacyFunction(int* ptr) {
    // Изменяет значение по указателю
    *ptr = 100;
}

void modernFunction(const int* ptr) {
    // Что здесь правильно?
    legacyFunction(const_cast<int*>(ptr));
}
```

**Варианты ответов:**
A) Код правильный, если исходный объект не был изначально const
B) Код всегда правильный
C) Код всегда неправильный, это UB
D) Нужно использовать `static_cast` вместо `const_cast`

---

## Вопрос 20 (Очень сложный)
Что произойдет при выполнении следующего кода?

```cpp
class Complex {
    mutable std::mutex mutex_;
    int data_;
public:
    int getData() const {
        std::lock_guard<std::mutex> lock(mutex_);
        return data_;
    }
    
    void setData(int value) {
        std::lock_guard<std::mutex> lock(mutex_);
        data_ = value;
    }
};

const Complex c;
c.setData(42);  // Что здесь?
int x = c.getData();
```

**Варианты ответов:**
A) Ошибка компиляции: нельзя вызывать неконстантный метод у const объекта
B) Код скомпилируется, но будет race condition
C) Код правильный и безопасный
D) Неопределенное поведение из-за mutable

---

## Вопрос 21 (Средний)
В чем разница между West const и East const стилями записи?

```cpp
const int* p1;   // East const
int const* p2;   // West const
```

**Варианты ответов:**
A) East const означает константный указатель, West const - указатель на константные данные
B) Нет разницы, оба варианта эквивалентны
C) West const устаревший стиль, не рекомендуется использовать
D) East const работает только в C, West const - только в C++

---

## Вопрос 22 (Средний)
Что означает `const char* const`?

```cpp
const char* const VERSION = "1.0.0";
```

**Варианты ответов:**
A) Константный указатель на константные данные
B) Указатель на константные данные, который можно переназначить
C) Константный указатель на изменяемые данные
D) Синтаксическая ошибка, правильный вариант `char const* const`

---

## Вопрос 23 (Средний)
В чем разница между логической и битовой константностью?

```cpp
class Cache {
    mutable std::map<std::string, int> cache_;
public:
    int getValue(const std::string& key) const {
        // Изменяем cache_, но логически объект не меняется
        cache_[key] = computeValue(key);
        return cache_[key];
    }
};
```

**Варианты ответов:**
A) Логическая константность - данные не меняются с точки зрения пользователя, битовая - физические биты не меняются
B) Логическая константность - физические биты не меняются, битовая - данные не меняются с точки зрения пользователя
C) Нет разницы, это синонимы
D) Логическая константность используется только в C, битовая - в C++

---

## Вопрос 24 (Средний)
Для чего используются методы `cbegin()` и `cend()`?

```cpp
class Container {
    std::vector<int> data_;
public:
    iterator begin() { return data_.begin(); }
    const_iterator begin() const { return data_.begin(); }
    const_iterator cbegin() const { return data_.begin(); }
};
```

**Варианты ответов:**
A) Для получения константных итераторов из неконстантного объекта
B) Для получения неконстантных итераторов из константного объекта
C) Устаревшие методы, не рекомендуется использовать
D) Для получения обратных итераторов

---

## Вопрос 25 (Средний)
Что означает `const` в range-based for цикле?

```cpp
std::vector<int> vec = {1, 2, 3};
for (const auto& item : vec) {
    // item = 10;  // Можно ли так?
}
```

**Варианты ответов:**
A) `const` означает что контейнер нельзя изменять
B) `const` означает что элементы нельзя изменять через `item`
C) `const` не имеет эффекта в этом контексте
D) `const` означает что цикл выполнится только один раз

---

## Вопрос 26 (Средний)
Когда правильно использовать `mutable` для кэширования?

```cpp
class Calculator {
    mutable std::optional<int> cached_result_;  // Правильно ли?
    int computeExpensive() const {
        if (!cached_result_) {
            cached_result_ = /* дорогие вычисления */;
        }
        return *cached_result_;
    }
};
```

**Варианты ответов:**
A) Правильно, т.к. кэш не меняет логическое состояние объекта
B) Неправильно, кэш должен быть обычным членом
C) Неправильно, нужно использовать `const_cast` вместо `mutable`
D) Правильно только если метод не `const`

---

## Вопрос 27 (Средний)
Когда НЕ следует использовать `mutable`?

```cpp
class Wrong {
    mutable int balance_;  // Правильно ли?
public:
    void withdraw(int amount) const {
        balance_ -= amount;  // Изменяем баланс в const методе
    }
};
```

**Варианты ответов:**
A) Правильно, т.к. используется `mutable`
B) Неправильно, т.к. изменение баланса меняет наблюдаемое состояние объекта
C) Правильно, если метод помечен как `const`
D) Неправильно, нужно использовать `const_cast`

---

## Вопрос 28 (Средний)
Безопасно ли преобразование non-const → const через `const_cast`?

```cpp
int x = 5;
int* px = &x;
const int* cpx = const_cast<const int*>(px);
```

**Варианты ответов:**
A) Всегда безопасно
B) Никогда не безопасно, это UB
C) Безопасно только если исходный объект был const
D) Безопасно только если исходный объект НЕ был const

---

## Вопрос 29 (Сложный)
Нужен ли `const` для возвращаемого значения по значению?

```cpp
class Value {
    int data_;
public:
    // Какой вариант правильный?
    const int getValue() const { return data_; }
    // или
    int getValue() const { return data_; }
};
```

**Варианты ответов:**
A) Оба варианта эквивалентны, но первый избыточен
B) Только первый вариант правильный
C) Только второй вариант правильный
D) Оба варианта неправильные

---

## Вопрос 30 (Сложный)
Как `const` влияет на оптимизацию компилятора?

```cpp
const int SIZE = 100;
int arr[SIZE];  // Использование константы

void process(const std::vector<int>& vec) {
    // Работа с vec
}
```

**Варианты ответов:**
A) `const` не влияет на оптимизацию, это только проверка на этапе компиляции
B) Компилятор может лучше оптимизировать код, зная что данные не изменятся
C) `const` замедляет выполнение программы из-за дополнительных проверок
D) `const` влияет только на thread-safety, но не на оптимизацию

---

## Вопрос 31 (Простой)
Какой вариант правильный для глобальной константы?

```cpp
// Вариант 1
char* VERSION = "1.0.0";

// Вариант 2
const char* VERSION = "1.0.0";

// Вариант 3
const char* const VERSION = "1.0.0";
```

**Варианты ответов:**
A) Вариант 1 правильный
B) Вариант 2 правильный
C) Вариант 3 правильный (константный указатель на константные данные)
D) Все варианты правильные

---

## Вопрос 32 (Средний)
Зачем использовать `const` для параметров функций, даже если функция не изменяет объект?

```cpp
void process(const User& user) {  // Зачем const?
    std::cout << user.getName();
}
```

**Варианты ответов:**
A) Для явного обозначения намерений - показать что объект не будет изменен
B) Для ускорения выполнения программы
C) Для обязательной проверки компилятором
D) const не нужен, это избыточно

---

## Правильные ответы

1. **B** - Значение переменной не может быть изменено после инициализации
2. **B** - `double getBalance() const { return balance_; }`
3. **B** - Ошибка компиляции: нельзя изменять константную переменную
4. **B** - `const User& user`
5. **B** - `const int*` - указатель на константные данные, `int* const` - константный указатель
6. **B** - Ошибка компиляции: нельзя изменять данные через const указатель
7. **C** - Только `getCount()` правильный (не изменяет состояние)
8. **A** - `mutable` позволяет изменять член класса в константном методе
9. **B** - `operator[]` нужен в двух версиях
10. **B** - Ошибка компиляции: нельзя изменять не-mutable члены в const методе
11. **B** - Выведет 10 (безопасно, т.к. x не был изначально const)
12. **D** - Неопределенное поведение (UB), т.к. x был изначально const
13. **B** - Только первый вариант правильный (возврат константной ссылки эффективнее)
14. **B** - Ошибка компиляции: сигнатуры методов не совпадают
15. **A** - Добавить `const_iterator begin() const` и `const_iterator end() const`
16. **C** - Код скомпилируется и выведет 1 (mutable позволяет изменение)
17. **C** - Оба оператора правильные как есть
18. **B** - Ошибка компиляции: нельзя присваивать константному объекту
19. **A** - Код правильный, если исходный объект не был изначально const
20. **A** - Ошибка компиляции: нельзя вызывать неконстантный метод у const объекта
21. **B** - Нет разницы, оба варианта эквивалентны
22. **A** - Константный указатель на константные данные
23. **A** - Логическая константность - данные не меняются с точки зрения пользователя, битовая - физические биты не меняются
24. **A** - Для получения константных итераторов из неконстантного объекта
25. **B** - `const` означает что элементы нельзя изменять через `item`
26. **A** - Правильно, т.к. кэш не меняет логическое состояние объекта
27. **B** - Неправильно, т.к. изменение баланса меняет наблюдаемое состояние объекта
28. **A** - Всегда безопасно (non-const → const всегда безопасно)
29. **A** - Оба варианта эквивалентны, но первый избыточен
30. **B** - Компилятор может лучше оптимизировать код, зная что данные не изменятся
31. **C** - Вариант 3 правильный (константный указатель на константные данные)
32. **A** - Для явного обозначения намерений - показать что объект не будет изменен

