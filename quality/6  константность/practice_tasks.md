# Практические задания по константности

## Задание 1: Кэширующий калькулятор
Реализуйте класс `CachingCalculator`, который будет кэшировать результаты сложных вычислений. 
Используйте константные методы и mutable для кэша.

```cpp
class CachingCalculator {
public:
    // Реализуйте методы:
    double getExpensiveComputation(int x) const;
    void clearCache() const;
private:
    // Добавьте необходимые поля
};
```

### Тесты:
1. Проверка, что первый вызов медленнее последующих
2. Проверка корректности кэширования одинаковых значений
3. Проверка работы с отрицательными числами
4. Проверка очистки кэша
5. Проверка работы с граничными значениями
6. Проверка многопоточного доступа
7. Проверка на переполнение кэша
8. Проверка exception safety
9. Проверка const correctness
10. Проверка работы с float point значениями

## Задание 2: Безопасный итератор
Создайте класс-обертку над std::vector, который предоставляет только константные итераторы и 
гарантирует неизменность данных при итерации.

```cpp
template<typename T>
class SafeContainer {
public:
    // Реализуйте методы:
    const_iterator begin() const;
    const_iterator end() const;
    void add(const T& value);
private:
    // Добавьте необходимые поля
};
```

### Тесты:
1. Проверка невозможности модификации через итератор
2. Проверка корректности итерации
3. Проверка работы с пустым контейнером
4. Проверка добавления элементов
5. Проверка const correctness
6. Проверка работы с пользовательскими типами
7. Проверка exception safety
8. Проверка инвалидации итераторов
9. Проверка копирования контейнера
10. Проверка многопоточного доступа

## Задание 3: Логгер с защитой от изменений
Реализуйте класс Logger, который позволяет логировать события, но защищает историю логов от изменений.

```cpp
class Logger {
public:
    // Реализуйте методы:
    void log(const std::string& message) const;
    const std::vector<std::string>& getHistory() const;
private:
    // Добавьте необходимые поля
};
```

### Тесты:
1. Проверка добавления логов
2. Проверка неизменности истории
3. Проверка многопоточного доступа
4. Проверка работы с пустыми сообщениями
5. Проверка работы с длинными сообщениями
6. Проверка ограничения размера истории
7. Проверка форматирования сообщений
8. Проверка exception safety
9. Проверка const correctness
10. Проверка производительности

## Задание 4: Иммутабельный конфиг
Создайте класс для хранения конфигурации, который гарантирует неизменность после инициализации.

```cpp
class Config {
public:
    // Реализуйте методы:
    static const Config& getInstance();
    int getValue(const std::string& key) const;
    bool hasKey(const std::string& key) const;
private:
    // Добавьте необходимые поля
};
```

### Тесты:
1. Проверка синглтона
2. Проверка неизменности конфига
3. Проверка отсутствующих ключей
4. Проверка типов значений
5. Проверка const correctness
6. Проверка многопоточного доступа
7. Проверка производительности
8. Проверка memory leaks
9. Проверка exception safety
10. Проверка сериализации

## Задание 5: Защищенный стек
Реализуйте класс стека, который гарантирует константность элементов после их добавления.

```cpp
template<typename T>
class ProtectedStack {
public:
    // Реализуйте методы:
    void push(const T& value);
    const T& top() const;
    void pop();
private:
    // Добавьте необходимые поля
};
```

### Тесты:
1. Проверка базовых операций
2. Проверка const correctness
3. Проверка работы с пользовательскими типами
4. Проверка exception safety
5. Проверка пустого стека
6. Проверка многопоточного доступа
7. Проверка утечек памяти
8. Проверка производительности
9. Проверка максимального размера
10. Проверка копирования и перемещения

## Задание 6: Константный итератор
Реализуйте свой класс константного итератора, который обеспечивает доступ только для чтения к элементам контейнера.

```cpp
template<typename Container>
class ConstIterator {
public:
    using value_type = typename Container::value_type;
    using reference = const value_type&;
    using pointer = const value_type*;
    
    // Реализуйте методы:
    reference operator*() const;
    pointer operator->() const;
    ConstIterator& operator++();
    bool operator==(const ConstIterator& other) const;
private:
    // Добавьте необходимые поля
};
```

### Тесты:
1. Проверка разыменования
2. Проверка инкремента
3. Проверка сравнения
4. Проверка доступа к членам через ->
5. Проверка работы с разными типами контейнеров
6. Проверка инвалидации
7. Проверка const correctness
8. Проверка копирования
9. Проверка присваивания
10. Проверка работы с пользовательскими типами

## Задание 7: Защищенный Observer
Реализуйте паттерн Observer с защитой от модификации наблюдателей во время уведомления.

```cpp
class Observer {
public:
    virtual void update(const string& message) = 0;
};

class Subject {
public:
    // Реализуйте методы:
    void attach(Observer* observer);
    void detach(Observer* observer);
    void notify(const string& message) const;
private:
    // Добавьте необходимые поля
};
```

### Тесты:
1. Проверка добавления наблюдателей
2. Проверка удаления наблюдателей
3. Проверка уведомления
4. Проверка const correctness
5. Проверка многопоточного доступа
6. Проверка порядка уведомлений
7. Проверка работы с nullptr
8. Проверка дублирования наблюдателей
9. Проверка утечек памяти
10. Проверка exception safety

## Задание 8: Иммутабельный билдер
Реализуйте паттерн Builder, который создает неизменяемые объекты.

```cpp
class Product {
public:
    // Реализуйте методы:
    static Builder getBuilder();
    const string& getName() const;
    int getValue() const;
private:
    // Добавьте необходимые поля
};

class Builder {
public:
    // Реализуйте методы:
    Builder& setName(string name);
    Builder& setValue(int value);
    const Product build() const;
private:
    // Добавьте необходимые поля
};
```

### Тесты:
1. Проверка базового построения
2. Проверка цепочки вызовов
3. Проверка неизменности продукта
4. Проверка значений по умолчанию
5. Проверка const correctness
6. Проверка копирования билдера
7. Проверка перемещения
8. Проверка exception safety
9. Проверка валидации входных данных
10. Проверка memory leaks

## Задание 9: Константный прокси
Реализуйте прокси-класс, который обеспечивает доступ только для чтения к объекту.

```cpp
template<typename T>
class ConstProxy {
public:
    // Реализуйте методы:
    explicit ConstProxy(T& obj);
    const T* operator->() const;
    const T& operator*() const;
private:
    // Добавьте необходимые поля
};
```

### Тесты:
1. Проверка доступа к методам
2. Проверка доступа к полям
3. Проверка const correctness
4. Проверка работы с указателями
5. Проверка работы со ссылками
6. Проверка копирования
7. Проверка присваивания
8. Проверка работы с разными типами
9. Проверка утечек памяти
10. Проверка exception safety

## Задание 10: Константный пул объектов
Реализуйте пул объектов, который предоставляет только константные объекты для использования.

```cpp
template<typename T>
class ConstPool {
public:
    // Реализуйте методы:
    const T& acquire() const;
    void release(const T& obj);
    size_t available() const;
private:
    // Добавьте необходимые поля
};
```

### Тесты:
1. Проверка получения объекта
2. Проверка возврата объекта
3. Проверка размера пула
4. Проверка многопоточного доступа
5. Проверка const correctness
6. Проверка работы с пользовательскими типами
7. Проверка exception safety
8. Проверка утечек памяти
9. Проверка производительности
10. Проверка переполнения пула 